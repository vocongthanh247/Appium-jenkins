name: Run Automation Task

on:
  schedule:
    - cron: '26 18 * * *'  # Chạy vào lúc 6:26 PM mỗi ngày

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run your automation script
        run: |
          echo "Chạy tự động hóa tại thời điểm này!"
