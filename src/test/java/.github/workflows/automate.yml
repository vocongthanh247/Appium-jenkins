name: Run Automation Task

on:
  schedule:
    - cron: '40 18 * * *'  # Chạy lúc 6h40 PM mỗi ngày

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run your automation script
        run: |
          echo "Chạy dự án vào thời gian này!"
